PACKAGE_NAME="wireview-hwmon"
PACKAGE_VERSION="1.3.1"
BUILT_MODULE_NAME[0]="wireview_hwmon"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"
MAKE[0]="make -C ${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build KDIR=${kernel_source_dir} module"
CLEAN="make -C ${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build KDIR=${kernel_source_dir} clean"
